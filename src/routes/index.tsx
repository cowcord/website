import { component$ } from '@builder.io/qwik';

export default component$(() => {
	return (
		<div class="bg-background flex h-[1500px] w-screen flex-col items-center justify-center">
			<p class="text-white">test</p>
		</div>
	);
});
